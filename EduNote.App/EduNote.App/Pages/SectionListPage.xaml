<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:b="clr-namespace:EduNote.App.Behaviours"
             xmlns:c="clr-namespace:EduNote.App.Converters"
             x:Class="EduNote.App.Pages.SectionListPage"
             Title="{Binding CurrentSection.Title}">
    <ContentPage.Resources>
        <c:ItemTappedConverter x:Key="ItemTappedConverter" />
    </ContentPage.Resources>
    <ListView ItemsSource="{Binding Sections}"
              SelectedItem="{Binding SelectedSection}"
              CachingStrategy="RecycleElement">
        <ListView.Behaviors>
            <b:EventToCommandBehavior EventName="ItemTapped" Command="{Binding ItemTappedCommand}" EventArgsConverter="{StaticResource ItemTappedConverter}" />
        </ListView.Behaviors>
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout Orientation="Vertical" Padding="10">
                        <Label Text="{Binding Title}" Style="{DynamicResource ListItemTextStyle}" FontSize="Large" />
                        <Label Text="{Binding Description}" Style="{DynamicResource ListItemDetailTextStyle}" FontSize="Small"/>
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>
